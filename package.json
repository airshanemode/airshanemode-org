{
  "name": "airshanemode-org",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "npm run build && npx @cloudflare/wrangler publish",
    "build":  "markdown posts/nypost-censorship.md --stylesheet ../styles/post.css --title 'How to Kill a Story in the USA' > public/posts/how-to-kill-a-story.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "airshanemode",
  "license": "MIT",
  "dependencies": {
    "@cloudflare/wrangler": "^1.11.0"
  },
  "devDependencies": {
    "markdown-to-html": "0.0.13"
  }
}
